import 'package:flutter/material.dart';

class newbutton extends StatelessWidget {
  const newbutton({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: SafeArea(child: Text("button")),
    );
  }
}